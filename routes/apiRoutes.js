const router = require("express").Router();
const fs = require("fs");

// router.get("/tableReserve", (req, res) => {
//   fs.readFile("./tabledata.js", "utf8", (err, data) => {
//     if (err) throw err;
//     res.data;
//   });
// });

console.log("hello");

module.exports = router;
